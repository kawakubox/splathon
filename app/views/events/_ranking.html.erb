<table class="table">
  <thead>
    <tr>
      <th>順位</th>
      <th>チーム</th>
      <th>試合数</th>
      <th>勝ち点</th>
    </tr>
  </thead>

  <tbody>
    <% teams.each do |team| %>
      <tr>
        <td><%= team.rank %></td>
        <td><%= team.name %></td>
        <td><%= team.match_count %></td>
        <td><%= team.points %></td>
      </tr>
    <% end %>
  </tbody>
</table>
